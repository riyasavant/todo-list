<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>Todo-list app</title>
        <link rel="stylesheet" href="css/style.css" type="text/css">
    </head>
    <body>
        <div class="box" id="heading">
            <h1> <%= title %> </h1>
        </div>
        
         <div class="box">
            <% for(var i=0; i<todoItems.length ; i++) { %>
                <form method="post" action="/delete">
                    <div class="item">
                        <input type="checkbox" onchange="this.form.submit()" name="deleteItem" value="<%= todoItems[i]._id %>">
                        <p> <%= todoItems[i].name %> </p>
                    </div>
                </form>
            <% } %>

            <form action="/" method="POST" class="item">
                <input type="text" name="item" placeholder="Add item" autocomplete="off">
                <button type="submit" name="button" value="pressed" class="btn btn-lg btn-outline-primary">+</button>
            </form>
         </div> 
        
            
        
        
    </body>
</html>